server:
  port: 9092

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/HasPatienceDB
  jpa:
    hibernate:
      ddl-auto: create
