server:
  port: 9093

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/HasAppointmentDB
  jpa:
    hibernate:
      ddl-auto: create
